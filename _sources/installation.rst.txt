.. _installation ::

Installation
************

Installing via PyPI
===================

It can be easily install through PyPI with the following command:

.. code-block:: bash

   $ pip install offpele
